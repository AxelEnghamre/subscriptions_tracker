const Notifications = ({
  className,
  id,
}: {
  className?: string;
  id: string;
}) => {
  return (
    <div className={className} id={id}>
      Notifications
    </div>
  );
};

export default Notifications;
