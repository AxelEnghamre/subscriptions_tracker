import type { Metadata } from "next";

const metadata: Metadata = {
  title: "Cookies",
};

const CookiesPage = () => {
  return <div>kakor</div>;
};

export default CookiesPage;

export { metadata };
